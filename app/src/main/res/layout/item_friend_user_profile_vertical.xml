<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="6dp"
    android:background="@color/white">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/civ_image_avatar_friend"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:src="@drawable/default_avatar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_name_friend"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:gravity="center"
        android:textColor="@color/color_text_bold"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@id/civ_image_avatar_friend"
        app:layout_constraintStart_toStartOf="@+id/civ_image_avatar_friend"
        app:layout_constraintTop_toBottomOf="@id/civ_image_avatar_friend" />

</androidx.constraintlayout.widget.ConstraintLayout>