<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- id item must be same with id fragment in navigation graph-->
    <item
        android:id="@+id/feedFragment"
        android:icon="@drawable/selector_bottom_nav_feed"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/groupFragment"
        android:icon="@drawable/selector_bottom_nav_group"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/chatFragment"
        android:icon="@drawable/selector_bottom_nav_chat"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/notificationFragment"
        android:icon="@drawable/selector_bottom_nav_notification"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/userProfileFragment"
        android:icon="@drawable/selector_bottom_nav_user_profile"
        tools:ignore="MenuTitle" />
</menu>